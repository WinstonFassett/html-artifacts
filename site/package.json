{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "data": "node scripts/extract-data.mjs",
    "sync": "node scripts/sync-assets.mjs",
    "dev": "npm run sync && portless html-artifacts astro dev",
    "dev:local": "npm run sync && astro dev",
    "build": "npm run sync && astro build",
    "preview": "portless html-artifacts astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.1",
    "astro": "^5.18.2",
    "tailwindcss": "^4.3.1"
  }
}
