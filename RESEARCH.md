# Single-File HTML Artifacts — Research Findings

> Round 1: 106-agent workflow, 23 sources, 111 claims, 25 verified (13 confirmed / 12 killed). Import maps, CDNs, browser support.
> Round 2: 100-agent workflow, 18 sources, 83 claims, 25 verified (16 confirmed / 9 killed). Agentic artifact ecosystem, platforms, skills, prior art.
> Date: 2026-06-12

---

## Summary

Single-file HTML artifacts that run with no build step are a well-established pattern. The core enabling technology is **ES module import maps**, baseline-stable across all major browsers since March 2023. The practical modern stack:

1. Inline `<script type="importmap">` block (hand-written or generated by JSPM)
2. `<script type="module">` using bare specifiers resolved to CDN URLs
3. Optionally, `esm.sh/tsx` (~1.7KB) for inline JSX/TypeScript

---

## Confirmed Findings

### 1. Import Maps — Browser Support
Import maps are baseline-stable across all major browsers (since March 2023). No polyfill needed for modern browsers; `es-module-shims` covers the ~4–6% on older browsers. Multiple-import-map support (Chrome 133+, Safari 18.4+) is still behind flags in Firefox as of mid-2026.

**Sources:** [MDN](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/script/type/importmap) · [SpiderMonkey blog](https://spidermonkey.dev/blog/2023/02/23/javascript-import-maps-part-1-introduction.html) · [Lit/Shoelace buildless guide](https://dev.to/matsuuu/buildless-workflow-through-import-maps-featuring-lit-shoelace-and-more-4ill)

### 2. Import Maps — Hard Constraints
Three rules practitioners must know:
1. The `<script type="importmap">` block **must appear before** any `<script type="module">` that uses its specifiers. Violating this throws: `"An import map is added after module script load was triggered."`
2. Import maps **only apply to `import` statements and `import()`** — not `<script src>`, workers, or worklets.
3. **Prefix remapping requires a trailing slash** on the map key (e.g., `"lit/": "https://..."` not `"lit"`).

**Sources:** [MDN](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/script/type/importmap) · [SpiderMonkey blog](https://spidermonkey.dev/blog/2023/02/23/javascript-import-maps-part-1-introduction.html)

### 3. JSPM Generator — Practical Tooling
JSPM provides two ways to generate import maps:
- **Visual generator** at [generator.jspm.io](https://generator.jspm.io/) — has an "HTML Boilerplate" checkbox (default: checked) that outputs a ready-to-run single HTML file.
- **Hosted API** at `https://api.jspm.io/generate` — GET or POST, returns `{ staticDeps, dynamicDeps, map }` JSON.

**Sources:** [generator.jspm.io](https://generator.jspm.io/) · [jspm.org/cdn/api](https://jspm.org/cdn/api)

### 4. esm.sh — Two Key Primitives
- **`esm.sh/tsx`** (~1.7KB): enables inline JSX/TypeScript via `<script type="text/babel|text/jsx|text/ts|text/tsx">` with zero build tooling.
- **`?external` parameter**: marks peer dependencies for browser-side import-map resolution rather than bundling them in. Example: `'react': 'https://esm.sh/react@19.2.4'` with `?external=react` and `*react-dom`.

> Note: The basic URL import pattern (`import React from 'https://esm.sh/react@18'`) was refuted 0–3 by adversarial verifiers. Use the import map + `?external` pattern instead and verify against current [esm.sh docs](https://esm.sh/).

**Source:** [github.com/esm-dev/esm.sh](https://github.com/esm-dev/esm.sh)

### 5. esm.run (jsDelivr ESM CDN) — Operational
`https://esm.run/lit` → 301 → `cdn.jsdelivr.net/npm/lit/+esm` serving Lit 3.3.3 ESM (verified June 2026). Pattern: `"lit": "https://esm.run/lit"` in import map.

> Caveat: esm.run explicitly states it is **not intended for production use** — convenience alias only, no SLA.

**Source:** [Lit/Shoelace buildless guide](https://dev.to/matsuuu/buildless-workflow-through-import-maps-featuring-lit-shoelace-and-more-4ill)

### 6. Vanilla No-Dep Pattern (juliensimon/browser-games)
The canonical zero-dependency game pattern: HTML5 Canvas + Web Audio API, no frameworks, no CDN imports. `juliensimon/browser-games` covers pong, asteroids, pac-man, space-invaders. Note: these use a 3-file structure (index.html + style.css + game.js), not true single-file. Pattern is still valid as a starting point for inlining.

**Source:** [github.com/juliensimon/browser-games](https://github.com/juliensimon/browser-games)

---

## CDN Summary

| CDN | URL Pattern | Production? | Notes |
|-----|-------------|-------------|-------|
| **esm.run** (jsDelivr) | `https://esm.run/PKG` | No (convenience alias) | 301 → jsdelivr, confirmed working |
| **esm.sh** | `https://esm.sh/PKG@VER` | Yes | `?external=` + `*` prefix for import maps; `/tsx` for in-browser TS |
| **jspm.io** | `https://ga.jspm.io/npm:PKG@VER/...` | Yes | JSPM generator produces these |
| **unpkg** | `https://unpkg.com/PKG?module` | Degraded | Historically popular, reliability concerns |

---

## Import Map Boilerplate

```html
<!DOCTYPE html>
<html>
<head>
  <script type="importmap">
  {
    "imports": {
      "lit": "https://esm.run/lit",
      "lit/": "https://esm.run/lit/"
    }
  }
  </script>
</head>
<body>
  <script type="module">
    import { html, LitElement } from 'lit';
    // ...
  </script>
</body>
</html>
```

For JSX/TypeScript (esm.sh/tsx):
```html
<script src="https://esm.sh/tsx"></script>
<script type="text/tsx">
  import { useState } from 'https://esm.sh/react@19'
  // JSX here
</script>
```

---

## Known Gaps / Open Questions

1. **Hosting platform constraints** (Claude artifacts, Observable, CodePen, Val Town, StackBlitz) — size limits, CSP headers, allowed CDNs — were **not confirmed** by any surviving claim. Need direct investigation.
2. **`file://` URL support** for import maps — does it work in Chrome/Firefox/Safari without a local server? Research was inconclusive (1-2 split, not confirmed).
3. **Production CDN recommendation** — esm.run discourages production use; need clarity on esm.sh vs jspm.io vs jsdelivr for production single-file artifacts.
4. **Genre/category taxonomy** — the claim that single-file HTML repos span 7+ genres was refuted 0-3. No primary-source taxonomy exists; genre lists are anecdotal.
5. **Curated GitHub awesome-lists** specifically for single-file HTML artifacts — not found. `github.com/topics/single-html-file` and `github.com/topics/single-file?l=html` exist but weren't confirmed as comprehensive collections.
6. **Vanilla HTML platforms** — are there tools specifically targeting vanilla HTML generation (not React/JSX) that have emerged in 2026?
7. **CDN library version corpus** — no systematic analysis of what Mermaid/Chart.js/Tailwind/Alpine CDN patterns AI tools actually emit in practice.

---

## Agentic HTML Artifact Ecosystem

> Round 2 findings — 100-agent workflow, 18 primary sources.

### Three-Tier Landscape

| Tier | Examples | Output | Build? |
|------|----------|--------|--------|
| Official Anthropic tooling | web-artifacts-builder skill | React+TS+Tailwind → bundle.html | Yes (Parcel+html-inline) |
| Community Claude skills | html-artifacts, html-anything, md2html, claude-design-skill | Pure self-contained HTML | No |
| Standalone platforms | vibes.diy, open-artifacts (mayfer), open-artifacts (13point5) | React ES modules / JSX-in-browser | No (esbuild-wasm in browser) |

### Platforms

**open-artifacts (mayfer fork)** — [github.com/mayfer/open-artifacts](https://github.com/mayfer/open-artifacts)
- Single static HTML file, zero build step
- Bundles user-prompted JSX/React in-browser via **esbuild-wasm** (loaded from jsDelivr)
- Resolves npm packages via CDN, executes in iframe
- LLM-agnostic: OpenAI, Ollama, Groq, OpenRouter active; Claude "coming soon"

**open-artifacts (13point5 fork)** — [github.com/13point5/open-artifacts](https://github.com/13point5/open-artifacts)
- MIT-licensed Next.js app, **archived October 2024**
- Supports Anthropic + OpenAI via user API keys
- Stack: Supabase, shadcn/ui, Vercel AI SDK

**vibes.diy** — [github.com/VibesDIY/vibes.diy](https://github.com/VibesDIY/vibes.diy)
- Generates **React ES modules, not vanilla HTML**
- System prompt requires `export default function App()` loaded as ES module at runtime
- Uses Tailwind CSS + **Fireproof** for local-first data persistence/sync
- Primary artifact is `App.jsx`; open source (VibesDIY org)

**Anthropic web-artifacts-builder skill** — [github.com/anthropics/skills](https://github.com/anthropics/skills/blob/main/skills/web-artifacts-builder/SKILL.md)
- Explicitly "not for simple single-file HTML/JSX artifacts"
- Stack: React 18 + TypeScript + Vite + Parcel + Tailwind CSS + shadcn/ui
- Bundle pipeline: Parcel → html-inline → `bundle.html` (all JS/CSS inlined)
- Last committed 2025-12-01

### Community Skills (Pure HTML, No Build)

**dogum/html-artifacts** — [github.com/dogum/html-artifacts](https://github.com/dogum/html-artifacts)
- Instructs Claude to produce a single `.html` file
- All CSS in `<style>`, all JS in `<script>`, images as inline SVG or data URIs
- Works offline; CDN use advisory not required
- Apache 2.0, created 2026-05-09

**nexu-io/html-anything** — [github.com/nexu-io/html-anything](https://github.com/nexu-io/html-anything)
- 75 composable skill templates across 9 surfaces:
  - Magazine articles, keynote decks, resumes, posters, tweet/Xiaohongshu cards, web prototypes, data reports, Hyperframes videos
- Streams AI output via SSE → appends to `iframe.srcdoc` with ~320ms debounce ("watch the AI draw")
- Updated 2026-06-12 (very active)

**haidang1810/md2html** — [github.com/haidang1810/md2html](https://github.com/haidang1810/md2html)
- Converts Markdown → self-contained HTML
- Works with Claude Code, Codex CLI, Antigravity
- No Node, Python, Docker, npm install required

**jiji262/claude-design-skill + Trystan-SA/claude-design-system-prompt**
- Both adapted from Claude.ai's leaked internal **Design product system prompt**
- Produce interactive clickable prototypes as single-file HTML (HTML+CSS+SVG+JS)
- jiji262 version restructures to Claude Skill spec with progressive disclosure

### Genre Map (AI-Generated HTML Artifacts)

Based on html-anything's 9 surfaces and observed community output:

| Genre | Libraries commonly used |
|-------|------------------------|
| Diagrams / flowcharts | Mermaid.js (CDN) |
| Data visualization | Chart.js (CDN), D3.js (CDN) |
| Interactive reports / dashboards | Chart.js, Tailwind, Alpine.js |
| Presentations / slideshows | Reveal.js (CDN) |
| UI prototypes | Tailwind, vanilla CSS/JS, SVG |
| Documents / articles | Marked.js, highlight.js, Tailwind |
| Mini-apps / calculators | Alpine.js, vanilla JS |
| Games / simulations | Canvas API, vanilla JS |
| Cards / social media visuals | Tailwind, inline SVG |

### Collections

- [madewithclaude/awesome-claude-artifacts](https://github.com/madewithclaude/awesome-claude-artifacts) — curated gallery of Claude-generated artifacts

---

## Sources Consulted

**Round 1 — Import maps, CDNs, browser support**
- [MDN import map reference](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/script/type/importmap)
- [SpiderMonkey: JavaScript Import Maps Part 1](https://spidermonkey.dev/blog/2023/02/23/javascript-import-maps-part-1-introduction.html)
- [Buildless workflow with Lit/Shoelace/import maps](https://dev.to/matsuuu/buildless-workflow-through-import-maps-featuring-lit-shoelace-and-more-4ill)
- [generator.jspm.io](https://generator.jspm.io/)
- [jspm.org/cdn/api](https://jspm.org/cdn/api)
- [esm.sh GitHub](https://github.com/esm-dev/esm.sh)
- [juliensimon/browser-games](https://github.com/juliensimon/browser-games)
- [Julia Evans: How to import a JS library](https://jvns.ca/blog/2024/11/18/how-to-import-a-javascript-library/)
- [No-build Todo app with htm + Preact](https://dev.to/ekeijl/no-build-todo-app-using-htm-preact-209p)
- [Zero-build React with ESM CDNs](https://dev.to/nitinajaydixit/going-back-to-basics-with-zero-build-react-the-ultimate-development-guide-to-modern-esm-cdns-with-1moh)
- [CodePen import maps blog post](https://blog.codepen.io/2026/05/06/chris-corner-import-maps/)
- [Observable notebooks retrospective (macwright.com)](https://macwright.com/2025/07/31/observable-notebooks-2)

**Round 2 — Agentic artifact ecosystem**
- [github.com/mayfer/open-artifacts](https://github.com/mayfer/open-artifacts)
- [github.com/13point5/open-artifacts](https://github.com/13point5/open-artifacts)
- [github.com/anthropics/skills — web-artifacts-builder](https://github.com/anthropics/skills/blob/main/skills/web-artifacts-builder/SKILL.md)
- [github.com/dogum/html-artifacts](https://github.com/dogum/html-artifacts)
- [github.com/nexu-io/html-anything](https://github.com/nexu-io/html-anything)
- [github.com/haidang1810/md2html](https://github.com/haidang1810/md2html)
- [github.com/jiji262/claude-design-skill](https://github.com/jiji262/claude-design-skill)
- [github.com/Trystan-SA/claude-design-system-prompt](https://github.com/Trystan-SA/claude-design-system-prompt)
- [github.com/VibesDIY/vibes.diy](https://github.com/VibesDIY/vibes.diy)
- [github.com/madewithclaude/awesome-claude-artifacts](https://github.com/madewithclaude/awesome-claude-artifacts)

---

## How vibes.diy runs React with no build (source-verified)

Inspected directly from the cloned `platforms/vibes.diy` source. The runtime is **Sucrase + import map + esm.sh fallback** — NOT esbuild-wasm or Parcel:

1. AI writes `App.jsx` (TSX, `export default function App()`)
2. **Sucrase** (`pkg/slack/transform-sucrase.ts`) strips TS types + transforms JSX → `React.createElement` *in the browser at runtime*. Sucrase is ~20KB, fast because it only strips/transforms, never type-checks.
3. **`bare-specifier-rewrite.ts`** rewrites any unrecognized bare import (`three`, `chart.js`, `tone`) → `https://esm.sh/<name>`. Only rewrites the top-of-file import region; specifiers already in the import map (or matched by trailing-slash prefix) are left untouched.
4. The transformed code runs as a blob module; `mount-vibes.ts` reuses a persistent React root so a broken intermediate render during streaming doesn't blank the iframe.

Key files: `vibes.diy/vibe/runtime/bare-specifier-rewrite.ts`, `pkg/slack/transform-sucrase.ts`, `vibe/runtime/mount-vibes.ts`.

---

## Round 3 — Interactive / diagram / viz (source-verified, all no-build from CDN)

| Library | Loading | CDN pattern | file://? |
|---------|---------|-------------|----------|
| **Mermaid** | ESM | `import mermaid from 'https://cdn.jsdelivr.net/npm/mermaid@11/dist/mermaid.esm.min.mjs'` | ✗ |
| **Chart.js** | script | `<script src="https://cdn.jsdelivr.net/npm/chart.js@4">` | ✓ |
| **Vega-Lite** | script | vega@6 + vega-lite@6 + vega-embed@7 | ✓ |
| **Plotly** | script | `<script src="https://cdn.plot.ly/plotly-3.0.0.min.js">` | ✓ |
| **D3** | ESM | `import * as d3 from 'https://cdn.jsdelivr.net/npm/d3@7/+esm'` | ✗ |
| **d3-graphviz** | script | d3.v7 + @hpcc-js/wasm@2 + d3-graphviz@5 (UMD) | ✓ |

Pulled real-world examples: bonomani/Mermaid-Viewer (zoom/pan/export, 14KB), KevinOBytes/mermaid-viewer (7 diagram types), magjac/d3-graphviz basic-unpkg.html (13 lines).

---

## Round 4 — Video / animation / decks (source-verified)

**Confirmed no-build from CDN:**

| Library | Type | Loading | file://? | CDN |
|---------|------|---------|----------|-----|
| **reveal.js** | deck | ESM or script | script-tag ✓ / ESM ✗ | `reveal.js@5/dist/reveal.esm.js` (CDN works but is **undocumented** — issue #2893) |
| **impress.js** | 3D deck | script | ✓ | `impress.js@1.1.0/js/impress.min.js` (NOTE: no @2 on npm) |
| **GSAP** | motion | script | ✓ | `gsap@3/dist/gsap.min.js` |
| **anime.js v4** | motion | ESM or UMD | ESM ✗ / UMD ✓ | `animejs@4/+esm` |
| **Motion** (motion.dev) | motion | ESM | ✗ | `motion@12/+esm` |
| **Lottie** | AE-JSON anim | script | ✓ | `lottie-web@5.12.2/lottie.min.js` |
| **Rive** | runtime anim | script | ✓ | `@rive-app/webgl2@2` |

**NOT no-build (excluded):**
- **Slidev** — requires Node/Vite toolchain; `slidev build` emits multi-file SPA; export targets only PDF/PPTX/PNG/MD, **never single-file HTML**.
- **Remotion** browser renderer (`renderMediaOnWeb`) — standalone-from-CDN viability **unverified** (refuted 1-2). Needs a React/Remotion project in practice.

**Hyperframes pattern** (html-anything `video-hyperframes`): N sequential `<section class="frame">` (1920×1080), each with hidden `<!-- frame:N duration:3000 transition:fade -->` markers, JS autoplay loop + progress bar, and a `HYPERFRAMES_META` JSON comment at the end — the same HTML doubles as a **Remotion script** to render an actual mp4. Source: heygen-com/hyperframes.

---

## CommonJS / file://-safe JSX (answer to "non-ESM way to do TSX")

Two ways to run JSX with **no ESM, works from `file://`**:

| Approach | Mechanism | Size | Notes |
|----------|-----------|------|-------|
| **Babel Standalone** | UMD globals + `<script type="text/babel" data-presets="react,typescript">` | ~2.7MB | Full JSX/TSX. React/ReactDOM from `window`. Slower (JS parse). |
| **htm** | Tagged template literals, `html\`<App/>\`` | ~600B | No transform at all. JSX-*like*, not real JSX. |

vs. **esm.sh/tsx** (ESM): ~1.7KB loader, real TSX, needs HTTP (uses `import.meta.url` + fetch). MUST be loaded as `<script type="module">` and needs `react/jsx-runtime` in the import map.

---

## Round 5 — Reactive view stacks + power libs (build-tested, not just researched)

The key insight: **any *runtime* React library works no-build via esm.sh + esm.sh/tsx.** Compiler-dependent tools are the only exceptions.

### View / reactive stacks

| Stack | Reactivity | Loading | file://? | Verdict |
|-------|-----------|---------|----------|---------|
| React (esm.sh/tsx) | hooks, real TSX | ESM | ✗ | ✅ tested |
| React (Babel Standalone) | hooks, real JSX | script | ✓ | ✅ tested |
| React (htm) | hooks, tagged templates | script | ✓ | ✅ tested |
| Preact + htm | hooks, 3KB | ESM | ✗ | ✅ tested (needs import map for externalized `preact`) |
| Solid + `solid-js/html` | fine-grained signals | ESM | ✗ | ✅ tested |
| Vue 3 (full global) | composition API | script | ✓ | ✅ tested |
| petite-vue | directives, 6KB | ESM | ✗ | ✅ tested |
| Alpine | directives | script | ✓ | ✅ tested |
| Lit | reactive web components | ESM | ✗ | ✅ tested (shadow DOM) |
| **Svelte** | — | — | — | ❌ **compiler-required** |

### Compiler-required gaps (the things that DON'T work no-build)

- **Svelte** — confirmed: `compile()` converts `.svelte` → JS module; raw `.svelte` can't run in-browser. `svelte-browser-import` bundles the compiler but uses `eval`, is dev-only, Svelte 4 only. Svelte 5 explicitly can't be CDN-bundled (maintainer: "Resolving all the imports necessary is a huge pain"). Source: sveltejs/svelte#15658, svelte.dev/docs/svelte-compiler.
- **Solid via TSX** — esm.sh/tsx uses swc's generic jsx-runtime; Solid needs compile-time `babel-preset-solid` for fine-grained reactivity. The TSX path would silently produce non-reactive Solid. **Use `solid-js/html` tagged templates instead.**
- **Remotion rendering** — Player previews in-browser fine (tested: composition plays with timeline/controls), but mp4 export needs Remotion's Node render toolchain.
- **BlockSuite** — confirmed browser-tested: its ~20 internal `@blocksuite/affine-*` packages can't be served coherently by esm.sh/jsdelivr (dual-package hazard, `REFERENCE_NODE` missing-export). Needs Vite. See Round 6.

### Power / capability libs (build-tested)

| Capability | Library | Loading | file://? | Verdict |
|-----------|---------|---------|----------|---------|
| Declarative 3D in React | React Three Fiber v9 | ESM via esm.sh/tsx | ✗ | ✅ tested — `?external=react,react-dom,three` |
| Programmatic video preview | Remotion Player v4 | ESM via esm.sh/tsx | ✗ | ✅ tested — needs `remotion/no-react` in import map |
| In-browser SQLite | SQL.js 1.14 | script + WASM | ✓ | ✅ tested — `locateFile` → CDN for the .wasm |
| Local-first persistence | Fireproof | ESM | ✗ | ✅ tested (persists to IndexedDB) |

### esm.sh externalization gotcha

When a package is loaded with `?external=react` (etc.), esm.sh keeps the bare specifier (`react`, `preact`, `remotion/no-react`) instead of inlining it — so you **must** provide an import map entry for each externalized specifier, including deep sub-paths the library imports internally (e.g. `remotion/no-react`, `preact/hooks`). Missing one → `Failed to resolve module specifier`.

---

## Round 6 — BlockSuite (AFFiNE's block editor): ❌ NOT no-build-able from CDN (browser-tested)

**Goal:** port the BlockSuite `preact-basic`/`react-basic` starters (from `~/dev/personal/try/blocksuite-examples`) to a single-file artifact.

**Verdict: impossible from public ESM CDNs.** This is a structural limitation, not a missing flag. Tested in a real headless browser against esm.sh and jsdelivr, BlockSuite **0.19.5** (esm.sh `latest`; repo is now on v0.22).

**Root cause — dual-package hazard in a tightly-coupled monorepo:**
BlockSuite is ~20 internal `@blocksuite/affine-*` packages (`affine-components`, `affine-block-surface`, `affine-model`, `affine-shared`, `block-std`, `inline`, …) that share singleton state (yjs, the block-std element registry, the inline editor) and import each other by **bare specifiers + subpaths**. esm.sh serves each as a *separate* module whose builds disagree on exports, so you hit:

```
The requested module '/@blocksuite/affine-components@0.19.5/.../rich-text.mjs'
does not provide an export named 'REFERENCE_NODE'
```

**Every escape hatch tried (all failed):**

| Strategy | Result |
|---|---|
| Per-package `?external=yjs,@blocksuite/store,…` import map | `REFERENCE_NODE` missing-export |
| Trailing-slash subpath prefix mappings (`@blocksuite/blocks/` → …) | drops `?external`, re-duplicates deps → same error |
| esm.sh `?bundle-deps` | esm.sh build fails outright (graph too big) |
| esm.sh `*@blocksuite/presets` standalone | back to bare-subpath `Failed to resolve` |
| esm.sh `?deps=` pinning all 9 internals to 0.19.5 | esm.sh build timeout |
| **jsdelivr `/+esm`** (different bundler) | fails to resolve too |

**The framework wrapper was never the blocker.** BlockSuite editors are just Lit web components — Preact/React only `appendChild(new PageEditor())`. So Preact and React fail *identically*; it's BlockSuite's own packaging. Correct 0.19 API for reference (needs Vite): `effects()` from `@blocksuite/blocks/effects` + `@blocksuite/presets/effects`, then `createEmptyDoc().init()` + `new PageEditor()`.

**Takeaway:** BlockSuite requires a real bundler (Vite — which every official starter uses). Adds to the compiler/bundler-required gap list alongside Svelte. If a no-build block editor is wanted, evaluate BlockNote / Editor.js / TipTap instead.

---

## Round 6 — What's actually POPULAR + the coolest real artifacts

### What people actually reach for (high confidence)

**The dominant artifact stack is fixed by Claude's environment:** React + Tailwind + shadcn/ui + Lucide React (icons) + **Recharts** (charts) + Three.js (r128, core only). It's the default *because* it's what Claude Artifacts ships pre-loaded — no install.

- **Recharts** is the featured chart lib — the ONLY one with a dedicated example in Claude's system prompt (not Chart.js/D3/Plotly). React-native, matches the React+Tailwind default.
- **Tailwind** in single-file artifacts = Play/browser CDN: `<script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4">` (v4) in `<head>`. v3 used `cdn.tailwindcss.com`. **Claude's sandbox blocks `cdn.tailwindcss.com`** — serve from cdnjs instead, or rely on Claude's pre-injected Tailwind.
- **Claude Artifacts CSP whitelist**: `cdnjs.cloudflare.com`, `cdn.jsdelivr.net/pyodide`, `fonts.googleapis.com`. Enables Pyodide (Python WASM), `@sqlite.org/sqlite-wasm`, jsQR, Fabric.js.
- **Artifacts can call LLMs + CORS APIs directly from the client** — no backend. The "AI inside the artifact" pattern.

**The vanilla counter-current** — Simon Willison's 216 single-file tools at tools.simonwillison.net, almost all LLM-generated, frequently prompted with **"no react"**. CDN-loaded deps, direct API/LLM calls from the client. This is the most-cited real-world body of agentic HTML artifacts.

Sources: leaked/reverse-engineered Claude Artifacts system prompt (reidbarber.com, the dedlim gist), Tailwind Play CDN docs, Simon Willison's "Everything I built with Claude Artifacts" (simonw.substack.com), claudio-silva/claude-artifact-runner.

### The coolest real artifacts (verified showcases)

- **tools.simonwillison.net** ([github.com/simonw/tools](https://github.com/simonw/tools)) — 216 single-file tools. **Vendored a curated 29 into `examples/simonw-tools/`.** Standouts: `v86` (x86 emulator in-browser), `micropython`/`quickjs` (language runtimes), `sqlite-wasm`, `pyodide-repl`/`numpy-pyodide-lab` (Python), `ocr`, and the LLM-from-inside-the-artifact ones (`haiku`, `omit-needless-words`, `gemini-chat`, `openai-webrtc`).
- **madewithclaude.com** — curated gallery (Tools/Design/Data/Graphics/Education/Programming/...). Homepage 403s to scrapers; medium confidence.
- **Anthropic in-app "Inspiration" gallery** — Learn something / Life hacks / Play a game / Be creative / Touch grass.

**Honest gap:** specific viral named artifacts (particle sims, generative art, tone.js music, explorable explanations) were NOT individually verifiable. The Nicky Case "explorable explanations" → artifact link was refuted. Simon's collection is the one concrete, link-backed body of work.

### Gallery perf note

With ~150 cards, eager-loading every preview iframe times out the `load` event and is noisy. The gallery uses an `IntersectionObserver` (300px rootMargin) so only ~12 iframes load on init; the rest load on scroll.

---

## Round 8 — CodeMirror 6 + Milkdown/Crepe no-build (hard-won, tested)

### The CodeMirror-6-from-CDN wall (and the fix)

CodeMirror 6 is ~8 packages (`@codemirror/state`, `/view`, `/commands`, `/language`, `/autocomplete`, `/lint`, `/search` + `@lezer/*`). A naive multi-package CDN load pulls **two copies of `@codemirror/state`**, and CM throws because `instanceof` checks fail across copies (same class-identity problem as Yjs).

**Two working recipes:**

1. **Deno pre-bundled ESM (simplest)** — dedup solved upstream, one import-map line:
   ```html
   <script type="importmap">{ "imports": { "codemirror/": "https://deno.land/x/codemirror_esm@v6.0.1/esm/" } }</script>
   <script type="module">
     import { basicSetup, EditorView } from 'codemirror/codemirror/dist/index.js';
     import { javascript } from 'codemirror/lang-javascript/dist/index.js';
   </script>
   ```
   Captured as `starters/codemirror-esm.html`. Tested.

2. **JSPM + `scopes` block** — verbose (~25 lines) but lets you pin exact per-package versions/themes/langs. Pattern: `ga.jspm.io/npm:codemirror@6.0.1/...` in `imports`, every `@codemirror/*` + `@lezer/*` in a `scopes["https://ga.jspm.io/"]` block. (Source: Winston's CodeSandbox recipes.)

### Milkdown Crepe in a single file (the non-obvious part)

`@milkdown/crepe` **statically imports `codemirror` at module-eval** — even with `features: { CodeMirror: false }`, the import graph still resolves it, and esm.sh's bundled `codemirror@^6` doesn't export `basicSetup`. Fix:
- Load crepe with `?external=codemirror,@codemirror/state,@codemirror/view,@codemirror/commands,@codemirror/language,@codemirror/search,@codemirror/autocomplete,@codemirror/lint` so esm.sh emits bare specifiers
- Map all those bare specifiers to the Deno bundle in the import map
- **CSS lives at `cdn.jsdelivr.net/npm/@milkdown/crepe@7/lib/theme/common/style.css` and `.../lib/theme/frame/style.css`** (NOT `esm.sh/.../theme/...` — that 404s)
- Crepe defaults are loud: override `.ProseMirror` padding + heading sizes

Captured as `starters/milkdown-dual-view-esm.html` (WYSIWYG ↔ raw markdown, localStorage). Tested: editor mounts, two-way sync works, zero errors/404s.

### The honest Yjs-collab gap

Single-file **Yjs collab with a rich editor (Crepe) does NOT work cleanly** — Yjs gets duplicated across 6 transitive packages (crepe, plugin-collab, y-prosemirror, y-trystero, y-indexeddb, y-protocols), and esm.sh `?external` dedup is whack-a-mole. `starters/collab-milkdown-yjs-esm.html` is shelved (WIP, not in gallery). Winston confirmed he's never done single-page Yjs+rich-editor collab either — it likely needs a bundler or an exhaustively-tuned import map. CodeMirror+Yjs collab (fewer transitive deps) is probably tractable; rich-editor collab is the real wall.

---

## Naming convention for this archive

- **`-esm` suffix** = uses ES module imports → needs an HTTP server (breaks from raw `file://` due to CORS on module loads)
- **no suffix** = plain `<script>` tags / UMD globals → runs directly from `file://`

The gallery (`index.html`) shows this as a badge per card: orange `ESM·http` / green `file://`.
